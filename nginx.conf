server {
  listen 80;
  server_name _;
  client_max_body_size 25M;
  location /api/ { proxy_pass http://server:4000/api/; proxy_http_version 1.1; proxy_set_header Host $host; }
  location /uploads/ { proxy_pass http://server:4000/uploads/; }
  location / { try_files $uri $uri/ /index.html; }
}
